# Playground

The static playground is meant for GitHub Pages and local preview. Open `playground.html` or publish the generated `docs/` bundle to expose the report layer without requiring a Python install first.
