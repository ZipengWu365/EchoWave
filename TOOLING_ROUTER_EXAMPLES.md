# Tooling router examples

- “Profile this dataset for modelling handoff.” → `ts_profile`
- “Compare these two curves and stop early if the signal is clear.” → `ts_compare`
- “Which tsontology entry point should I call before forecasting?” → `ts_route`
