# Visual reports

Use `profile.to_html_report()` to generate a standalone report page with:

- executive summary
- structural radar
- top-axis bars
- next actions
- compact agent context

Use `similarity_report.to_html_report()` for pairwise similarity pages.
